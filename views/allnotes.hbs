 {{> head}}
 
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                {{> menugood}}                  
            </div>
            <div class="col-md-12">
                   <h2 class="text-center">Заметки пользователя {{user}}</h2>               
            </div>
            <div class="col-md-12">
                <table class="table table-striped">
                    {{#each result}}
                    <tr>
                        <td style="width:80%">{{this.text}}</td>                      
                        <td><a class="btn btn-default" href="/deletenotes/{{this._id}}" role="button">Удалить</a></td>      
                        <td><a class="btn btn-default" href="/editnotes/{{this._id}}" role="button">Изменить</a></td>
                    </tr>
                    
                    {{/each}}
                </table>
                    
            </div>
        </div>
            
    </div>
    <!--<script>
        function DeleteNote(id) {
            $.ajax({
                url: "notes/"+id,
                contentType: "application/json",
                method: "DELETE",
                success: function (note) {
                    location.reload();                  
                }
            })
        }
        // нажимаем на ссылку Удалить
        $("body").on("click", ".remove", function () {
            var id = $(this).data("id");
            DeleteNote(id);
        })
    </script>-->
 {{> footer}}